/* ── Scroll pacing (LinkedIn virtual list) — card pacing removed ───────── */

const SCAN_DELAYS = {
  fast: { scroll: [1000, 2000] },
  normal: { scroll: [2000, 4000] },
  slow: { scroll: [4000, 7000] },
};
