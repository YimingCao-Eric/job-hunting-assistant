DATABASE_URL=postgresql+asyncpg://jha:jha@postgres:5432/jha
CONFIG_PATH=/app/data/config.json
PROFILE_PATH=/app/data/profile.json
EXTENSION_ORIGIN_REGEX=^chrome-extension://[a-zA-Z0-9]+$
DEDUP_COSINE_BATCH_SIZE=1000

# OpenAI API key: Step B JD extraction, Step D LLM scoring, profile skill extraction,
# and resume parsing (when those features use the LLM).
OPENAI_API_KEY=

# Optional: post-scrape auto-scrape wake (Redis subscriber in backend). When unset, wake-orchestrator publish is best-effort no-op for subscribers.
# REDIS_URL=redis(s)://host:6379/0
